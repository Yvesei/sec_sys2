filebeat.autodiscover:
  providers:
    - type: docker
      hints.enabled: true
      host: "unix:///var/run/docker.sock"

processors:
  - add_docker_metadata:
      host: "unix:///var/run/docker.sock"

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]